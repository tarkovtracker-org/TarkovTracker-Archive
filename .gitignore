# OS junk — not part of source
.DS_Store

# Logs — dev/runtime noise you can always regenerate
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local runtime data — developer-only scratch
local_data/

# Dependencies — installed from lockfiles
**/node_modules/

# Build output — compiled artifacts
frontend/dist/
functions/lib/

# TypeScript cache — speeds incremental builds
*.tsbuildinfo

# Tool caches — ephemeral build tooling state
.cache/

# Coverage reports — produced by test runs
**/coverage/

# Test artifacts — reports/output from test runners
**/test-results/
**/playwright-report/
**/.playwright/

# Firebase (local emulators & exports) — machine-specific state
.firebase/
.firebase-emulator-savedata/
firebase-export-*
.firebaserc

# Firebase function artifacts — generated file
functions/openapi/openapi.json

# AI assistant work dirs — local tooling state
.gemini/
.factory/
.claude/

# IDE/editor cruft — personal settings & temp files
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files — usually contain secrets; keep out of git
# (These two lines ignore .env, .env.local, .env.test, etc. anywhere)
**/.env

# If your .env.test files are SAFE (no secrets) and must be committed,
# uncomment the next line to include them:
!**/.env.test

# Misc project-specific ignores
bmad/

# Temporary files — throwaway patches, etc.
*.patch
