############################################################
# Unified Environment Template
# ----------------------------------------------------------
# Copy this file to `.env` for production builds or `.env.local`
# for local development. The app now uses a single Firebase
# project for every environment; while running on localhost it
# automatically connects to the Firebase emulators so your data
# stays local unless you explicitly disable them.
############################################################

# ----------------------------------------------------------
# Firebase configuration (required)
# ----------------------------------------------------------
VITE_FIREBASE_API_KEY=your-firebase-api-key
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=000000000000
VITE_FIREBASE_APP_ID=1:000000000000:web:your-app-id
VITE_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# ----------------------------------------------------------
# Developer toggles (optional)
# ----------------------------------------------------------
# Enables the mock authentication plugin so you can browse the UI
# without running Firebase Auth. Leave `false` to use real auth.
VITE_DEV_AUTH=false

# Adjust the client-side log level. Defaults to `debug` in dev and
# `warn` in production when unset. Accepted values: debug, info, warn, error.
# VITE_LOG_LEVEL=info

# Microsoft Clarity project identifier. If omitted the build falls
# back to the default project configured in `usePrivacyConsent.ts`.
# VITE_CLARITY_PROJECT_ID=your-clarity-project-id
