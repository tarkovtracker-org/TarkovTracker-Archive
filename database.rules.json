{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid && auth.uid !== null",
        ".write": "$uid === auth.uid && auth.uid !== null && newData.child('lastUpdated').val() === now"
      }
    }
  }
}
